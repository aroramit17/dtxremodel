# DTX Remodel

Static editorial redesign for DTX Remodel, a Dallas-area residential and commercial remodeling firm.

## Local preview

Open `index.html` directly in a browser, or run a local static server:

```bash
python3 -m http.server 8080
```

Then visit `http://localhost:8080`.
